{
  "name": "sql-assistant-netlify",
  "version": "1.0.0",
  "private": true,
  "description": "SQL Assistant rewritten in JavaScript for Netlify deployment",
  "scripts": {
    "dev": "netlify dev",
    "start": "netlify dev",
    "check": "node -e \"require('./netlify/functions/auth'); require('./netlify/functions/config'); require('./netlify/functions/generate'); console.log('ok')\"",
    "generate:key": "node scripts/generate-key.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.4.5",
    "openai": "^4.68.1"
  },
  "devDependencies": {
    "netlify-cli": "^20.1.0"
  }
}
